name: Funções GitHub Actions
on:
  workflow_dispatch:
    inputs:
      valor:
        default: "github"
        type: string
jobs:
  teste:
    permissions: write-all
    runs-on: ubuntu-latest
    steps:
      - name: Função "Contains"
        run: echo ${{ contains(inputs.valor, 'github') }}